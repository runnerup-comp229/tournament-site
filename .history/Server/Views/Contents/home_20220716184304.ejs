<div class="container">
    <div class="jumbotron">
        <h1 class="display-4">RunnerUP Landing Page</h1> 
         <h1 class="display-5">Welcome to RunnerUP tournament site!</h1>
      </div>
      <div class="row">
        <div class="col">
            <a href="/add" id="addButton" class="btn btn-primary mb-1"><i class="fa-solid fa-circle-plus"></i> Add Tournament</a>
           <table class="table table-striped table-bordered table-hover">
            <thead class="table-dark">
                <tr>
                    <th scope="col" class="text-center">#</th>
                    <th scope="col">Tournament Name</th>
                    <th scope="col">Owner</th>
                    <th scope="col">Active</th>
                </tr>
            </thead>
            <tbody id="contactList">
                <!-- Repeatable Rows -->
                <% for(let count = 0; count < tournament.length; count++) { %> 
                    
                    <tr>
                        <th scope="row" class="text-center"> <%= count %></th>
                        <td><%= tournament[count].Name %> </td>
                        < ><%= tournament[count].Owner %> </td>
                        <% if(tournament[count].isActive){ %>
                            <td>Yes</td>
                          <% } else{ %>  
                            <td>No</td>
                         <% } %>  
                    </tr>
                <% } %>     
            </tbody>
           </table>
        </div>
    </div>
</div>
