<div class="container">
    <h1><%=tournament.Name%> Semi-Final</h1>
    <div class="row">
        <div class="col">
            <a href="/add" id="addButton" class="btn btn-primary mb-1"><i class="fa-solid fa-circle-plus"></i> Add Team</a>
           <table class="table table-striped table-bordered table-hover">
            <thead class="table-dark">
                <tr>
                    <th scope="col" class="text-center">#</th>
                    <th scope="col">Team 1</th>
                    <th scope="col"></th>
                    <th scope="col">Team 2</th>
                </tr>
            </thead>
            <tbody id="contactList">
                <!-- Repeatable Rows -->
                <% let index = 1; %>
                <% for(let count = 0; count < 4; count += 2) { %> 
                    
                    <tr>
                        <th scope="row" class="text-center"> <%= index %> <%index += 1; %></th>
                        <td><%= tournament.RunnerUp[count]%> </td>
                        <td class="text-center">vs</td>
                        <td><%= tournament.RunnerUp[count + 1]%> </td>
                    </tr>
                <% } %>     
            </tbody>
           </table>
        </div>
    </div>
</div>